# Documentation placeholder — gi-docgen integration.
# Enable when gi-docgen is available on the build system.
#
# gi_docgen = find_program('gi-docgen', required: false)
# if gi_docgen.found()
#   gnome = import('gnome')
#   gnome.gtkdoc('gsni', ...)
# endif
